# TODO

- Add semantic photography search with build-time image indexing and in-browser query ranking.
- Add a local gallery pipeline that generates `thumb` and `lg` images into the repo, uploads originals to Google Drive, sets sharing permissions, and writes Drive download links into gallery data.
